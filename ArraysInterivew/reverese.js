str = "hello my name is jeff";

const reverse = (str) => {
  if (!str) {
    return "no input";
  }

  const split = str.split()
  const middleIndex = Math.ceil(split.length / 2);
  for(let i = 0; i < 0 split.length; i++) {
    

  }
};
